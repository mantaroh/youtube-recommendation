node_modules/
dist/
coverage/

# WXT / extension build output
.output/
.wxt/

# Cloudflare
.wrangler/

# Credentials (never commit)
.env
.env.local
.env.*.local

# Model cache used by local embedding runs
.models/

*.log
.DS_Store
Thumbs.db

# Local wrangler secrets
.dev.vars

# Credentials dropped in the working tree. Never commit these: a key in git history
# stays there after the file is deleted, and has to be rotated rather than removed.
youtube-api-token.txt
youtube-oauth-key.txt
*-key.txt
*-token.txt

# Python bytecode
__pycache__/
*.pyc

# Runner credentials: the worker bearer token and the Access service token.
engine-credentials.env

# Database exports taken before a migration. They hold the ratings.
.backups/
